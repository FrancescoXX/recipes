# Laravel Jetstream — Stage Environment
This is a stage environment for [Laravel Jetstream (info + deploy)](https://app.zerops.io/recipes/laravel-jetstream?environment=stage) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Stage** environment uses the same configuration as production,
but runs on the lowest scaling settings.
<!-- #ZEROPS_EXTRACT_END:intro# -->
