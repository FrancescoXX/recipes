# Zerops recipes repository
Containes descriptions and import YAML structures for all Zerops recipes. Applications (and their build and deploy configurations — `zerops.yaml`) used within these recipes have their own GitHub org [zerops-recipe-apps](https://github.com/zerops-recipe-apps).
